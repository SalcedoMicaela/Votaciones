services:
  - type: web
    name: voting-system-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGO
        sync: false
      - key: MONGO_DB
        sync: false
      - key: ADMIN_PASSWORD
        sync: false
